name: Build iOS App

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout kód
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'

    - name: Inštalácia závislostí
      run: npm install

    - name: Build Webovej aplikácie
      run: npm run build

    - name: Pridanie iOS platformy
      run: |
        npm install @capacitor/ios
        # Zabezpečíme, že capacitor.config.ts/json existuje
        npx cap init "Auto Manazer" "com.petmal819.automanazer" --web-dir dist || true
        # Pridáme iOS platformu
        npx cap add ios

    - name: Sync Capacitor iOS
      run: npx cap sync ios

    - name: Build iOS App (Nepodpísaná)
      run: |
        cd ios/App
        # Odstránil som pod install, npx cap sync ios by to mal urobiť sám
        xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build CONFIGURATION_BUILD_DIR=$PWD/build || {
          echo "XCODEBUILD ZLYHAL! Tu je detailný log:"
          xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build CONFIGURATION_BUILD_DIR=$PWD/build | tail -n 50
          exit 1
        }

    - name: Vytvorenie IPA súboru
      run: |
        cd ios/App/build
        mkdir -p Payload
        mv App.app Payload/
        zip -r app-unsigned.ipa Payload

    - name: Nahranie hotového súboru (Artifact)
      uses: actions/upload-artifact@v4
      with:
        name: ios-app-unsigned
        path: ios/App/build/app-unsigned.ipa
        retention-days: 5
